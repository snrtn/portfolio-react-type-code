import React from 'react';
import { Container, Wrapper } from '../../Styles/faq.styles';

interface Props {}

const FAQ = (props: Props) => {
  return (
    <Container>
      <Wrapper></Wrapper>
    </Container>
  );
};

export default FAQ;
