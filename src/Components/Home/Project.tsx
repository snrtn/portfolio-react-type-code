import React from 'react';
import { Container, Info } from '../../Styles/project.styles';

interface Props {}

const Project = (props: Props) => {
  return (
    <Container>
      <Info>C'est en train d'être construit.</Info>
    </Container>
  );
};

export default Project;
